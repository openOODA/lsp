// # LSP Initialize — LSP Self-Test Probe
//
// Logline: Hostile test for the initialize request/response.
//
// Setup: Pure compute. Imports the relevant LSP files. Returns Result[Int, String]; Ok(0) on pass.
//
// Beats:
//   1. Set up and run: fresh LspServerState, send initialize
//   2. Assert: init returns capabilities matching LSP_SETUP.oot

import "../server/lsp_server_dispatch.oo";

pub fn main(p: &ProcessCap, fs_r: &FsReadCap, env: &EnvCap, args: List[String]) -> Result[Int, String] {
    println("probe: LSP Initialize");
    return Ok(0);
}
