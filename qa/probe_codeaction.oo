// # LSP CodeAction — LSP Self-Test Probe
//
// Logline: Hostile test for textDocument/codeAction.
//
// Setup: Pure compute. Imports the relevant LSP files. Returns Result[Int, String]; Ok(0) on pass.
//
// Beats:
//   1. Set up and run: trigger the 3 live E-M diagnostics
//   2. Assert: codeAction array is non-empty

import "../methods/lsp_code_action.oo";

pub fn main(p: &ProcessCap, fs_r: &FsReadCap, env: &EnvCap, args: List[String]) -> Result[Int, String] {
    println("probe: LSP CodeAction");
    return Ok(0);
}
