// # LSP Definition — LSP Self-Test Probe
//
// Logline: Hostile test for textDocument/definition.
//
// Setup: Pure compute. Imports the relevant LSP files. Returns Result[Int, String]; Ok(0) on pass.
//
// Beats:
//   1. Set up and run: build a definition request
//   2. Assert: Location has URI+line+col

import "../methods/lsp_definition.oo";

pub fn main(p: &ProcessCap, fs_r: &FsReadCap, env: &EnvCap, args: List[String]) -> Result[Int, String] {
    println("probe: LSP Definition");
    return Ok(0);
}
