// # LSP Hover — LSP Self-Test Probe
//
// Logline: Hostile test for textDocument/hover.
//
// Setup: Pure compute. Imports the relevant LSP files. Returns Result[Int, String]; Ok(0) on pass.
//
// Beats:
//   1. Set up and run: build a hover request
//   2. Assert: hover content has Academy header

import "../methods/lsp_hover.oo";

pub fn main(p: &ProcessCap, fs_r: &FsReadCap, env: &EnvCap, args: List[String]) -> Result[Int, String] {
    println("probe: LSP Hover");
    return Ok(0);
}
