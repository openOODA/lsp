// # LSP Engine Version (Single Source of Truth)
//
// Logline: The canonical LSP engine version string consumed by the executeCommand handler and the double-run proof.
//
// Setup: Pure constant. No cap, no I/O. Callers: executeCommand, initialize serverInfo, CLI --version, and the double-run probe.
//
// Beats:
//   1. Return the LSP engine version string.

pub fn lsp_facade_version() -> String { return "0.6.28"; }
